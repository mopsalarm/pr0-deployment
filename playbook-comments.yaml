
- hosts: comments-hosts
  serial: 1
  roles:
    - role: deploy-service
      service_name: pr0gramm-comments
      service_port: 15041
      service_docker:
        image: mopsalarm/pr0gramm-comments:latest

        env:
          DATADOG_API_KEY: "{{ datadog_api_key }}"
          DATADOG_APP_KEY: "{{ datadog_app_key }}"
          POSTGRES_HOST: "{{ postgres_host }}"

        ports:
          - "15041:8080"
